# File-to-Spec Mapping

| File path | Module / feature | Mapped requirement IDs | Why this file matters | Main exported functions / component responsibility | Risk if changed |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `src/context/WMSContext.jsx` | State Management & Workflow Orchestration | WMS-INB-001, WMS-INB-003, WMS-INV-001, WMS-INV-002, WMS-OUT-001, WMS-OUT-002, WMS-MAS-001, WMS-INT-001 | Central hub for all WMS business logic, ledger recording, and ERP integration calls. | `WMSProvider`, `useWMS`, `submitInboundDraft`, `toggleHold`, `createWave`, `suggestLocations`, `startCycleCount`, `submitCountLine`, `getConvertedQty`, `syncMasterData` | High - Changes can break core business logic and state synchronization across all modules. |
| `src/services/wmsApi.js` | ERP Integration Layer | WMS-INT-001 | Defines the API contract and manages fallback mocks. Vital for phase 3 API handoff. | `wmsApi` (includes `submitReceipt`, `releaseWave`, `postAdjustment`, `syncFromErp`) | Medium - Defines the boundary between UI and real backend. Needs care when real API URLs are introduced. |
| `src/components/InboundDraftLayer.jsx` | Inbound Dynamic Receiving UI | WMS-INB-001, WMS-INB-002, WMS-INB-003 | Provides scanner-optimized interface for warehouse workers during receipt, including exception processing. | `InboundDraftLayer` | High - Critical operational screen. Breaking it halts warehouse receiving. |
| `src/components/PutawayTaskLayer.jsx` | Smart Putaway UI | WMS-OUT-002 | Directs warehouse workers to optimal storage locations and validates destination scans. | `PutawayTaskLayer` | Medium - If broken, putaway is delayed or goes to wrong locations. |
| `src/components/OnHandLayer.jsx` | Inventory Management UI | WMS-INV-001 | Real-time visibility into warehouse stock and provides Hold/Unhold administrative controls. | `OnHandLayer` | Medium - Breaks inventory visibility and quality lock mechanisms. |
| `src/components/CycleCountLayer.jsx` | Cycle Count Scanner UI | WMS-INV-002 | Dual mode component (Manager review + Worker scanner flow) for blind inventory counts. | `CycleCountLayer` | Medium - Directly impacts inventory accuracy and auditing. |
| `src/components/MasterDataLayer.jsx` | Master Data & UOM Management UI | WMS-MAS-001, WMS-INT-001 | Manages ERP item parameters, custom unit conversions, and triggers manual ERP sync. | `MasterDataLayer` | Low/Medium - Affects master data configuration, impacts receiving if units are misconfigured. |
| `src/mockData/master/locations.json` | Master Data (Locations) | WMS-OUT-002 | Stores zone configuration, operational flags (`canReceive`, `canPick`), and capacity limits. | N/A (JSON data) | Medium - Incorrect flags will route goods improperly. |
| `src/mockData/master/items.json` | Master Data (Items) | WMS-OUT-001, WMS-INB-001 | Stores base UOM, Lot control flags, and FEFO/FIFO picking strategies. | N/A (JSON data) | Medium - Breaks wave generation logic if strategies are missing. |
| `src/mockData/wms/onHand.json` | Inventory Data | WMS-OUT-001, WMS-OUT-002 | Mock initial state for inventory balances, locations, and expiry dates. | N/A (JSON data) | Low - Only affects initial demo state. |
